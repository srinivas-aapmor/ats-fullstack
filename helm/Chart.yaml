apiVersion: v2
name: fullstack-app
version: 0.1.0
description: A Helm chart for deploying React frontend and Flask backend

# Helm chart type
# 'application' is for deployable charts
# 'library' is for reusable charts
# Here, we use 'application'
type: application 